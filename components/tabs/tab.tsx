import { ReactNode } from "react";
import styled from "styled-components";

type TabProps = {
  label: string;
  children: ReactNode;
};

export const Tab = ({ label }: TabProps) => {
  return <div>tab</div>;
};
